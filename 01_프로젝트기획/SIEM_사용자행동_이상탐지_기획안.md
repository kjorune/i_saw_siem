# 사용자 행동 기반 보안 이상탐지 프로젝트 가기획안

## 1. 프로젝트 개요

### 1.1 프로젝트명

**사용자별 정상 행동 기준선과 Isolation Forest를 활용한 설명 가능한 이상 사용자 탐지**

부제:

**로그인·파일·USB 사용 기록을 중심으로 한 내부자 정보유출 위험 분석**

### 1.2 프로젝트 한 줄 정의

로그인, 파일, USB 로그를 사용자별·일자별로 정리한 뒤 평소 행동과 다른 사용자를 탐지하고, 위험하다고 판단한 이유와 시간순 행동 흐름을 보여주는 보안 이상탐지 PoC를 개발한다.

### 1.3 개발 조건

| 구분 | 내용 |
|---|---|
| 개발 기간 | 3주(평일 15일 기준) |
| 개발 인원 | 4명 |
| 프로젝트 성격 | 기업연계 데이터 분석 PoC |
| 주요 분석 단위 | 사용자 × 날짜 |
| 핵심 데이터 | `logon.csv`, `file.csv`, `device.csv`, LDAP |
| 선택 데이터 | `email.csv`, `http.csv` |
| 핵심 모델 | Isolation Forest |
| 최종 결과 | 위험 사용자 순위, 판단 근거, 행동 타임라인, 성능평가 |

---

## 2. 기획 배경

기업 내부의 정보유출이나 계정 오용은 하나의 로그만으로 판단하기 어렵다. 예를 들어 야간 로그인 자체는 정상일 수 있고, USB 연결이나 파일 접근도 각각은 일반적인 업무 행동일 수 있다. 그러나 평소와 다른 시간에 로그인한 사용자가 낯선 PC에서 다수의 파일에 접근한 뒤 USB를 연결했다면 확인이 필요한 행동 흐름으로 볼 수 있다.

기존 기획안은 사용자·IP·자산 기준선, 실시간 탐지, 공격 단계 추정, Incident 자동 생성 및 공격 그래프까지 포함해 구현 범위가 넓었다. 본 프로젝트에서는 제공된 샘플 로그로 완성 가능한 수준에 집중한다.

```text
원본 CSV 정리
→ 사용자별 하루 행동 요약
→ 정상 행동 기준선 생성
→ Isolation Forest 이상치 탐지
→ 규칙 기반 위험 근거 생성
→ 위험 사용자와 행동 흐름 시각화
→ 탐지 결과 검증
```

---

## 3. 프로젝트 목적

### 3.1 사용자별 정상 행동 파악

모든 사용자에게 동일한 기준을 적용하지 않고, 각 사용자의 과거 행동을 바탕으로 다음 정상 범위를 만든다.

- 주로 로그인하는 시간
- 주로 사용하는 PC
- 하루 평균 로그인 횟수
- 하루 평균 파일 활동량
- USB 사용 빈도

### 3.2 이상 사용자 탐지

사용자별 하루 행동을 Feature로 변환하고 Isolation Forest를 적용해 정상 패턴에서 크게 벗어난 사용자·날짜를 찾는다.

### 3.3 판단 근거 제공

모델 점수만 보여주지 않고 다음과 같은 근거를 함께 출력한다.

> 평소보다 3시간 늦은 로그인 / 처음 사용하는 PC / 파일 활동량 평소 대비 4.2배 / USB 연결 발생

### 3.4 의심 행동 흐름 확인

탐지된 사용자의 원본 로그를 시간순으로 정렬해 로그인부터 파일 접근, USB 사용까지의 흐름을 확인한다.

---

## 4. 핵심 분석 시나리오

### 메인 시나리오: 내부자 또는 계정 오용에 의한 파일 반출 위험

```text
비정상 시간 로그인
→ 평소 사용하지 않던 PC 접속
→ 파일 접근·복사량 급증
→ USB 장치 연결
→ 정보유출 위험 사용자로 분류
```

각 단계가 반드시 모두 발생해야 위험한 것은 아니다. Isolation Forest로 전체 행동의 이상도를 측정하고, 규칙으로 구체적인 위험 근거를 보완한다.

### 보조 시나리오

1. 야간 또는 주말의 대량 파일 활동
2. 평소 USB를 사용하지 않던 사용자의 갑작스러운 USB 사용
3. 여러 PC를 번갈아 사용하며 파일에 접근한 행동
4. USB 연결 직전·직후 파일 활동 급증

---

## 5. 활용 데이터

| 파일 | 주요 역할 | 우선순위 |
|---|---|---:|
| `logon.csv` | 로그인 시간, 사용자, PC, 로그인·로그아웃 행동 | 필수 |
| `file.csv` | 사용자별 파일 생성·접근·복사 등 활동량 | 필수 |
| `device.csv` | USB 연결·해제 행동 | 필수 |
| LDAP 월별 파일 | 사용자 부서, 직무, 조직 상태 등 사용자 정보 | 필수 또는 보완 |
| `email.csv` | 외부 메일 및 첨부 전송 정황 | 선택 |
| `http.csv` | 외부 업로드 사이트 접속 정황 | 선택 |
| `psychometric.csv` | 사용자 성향 정보 | 기본 범위 제외 |

`psychometric.csv`는 개인정보·편향 문제와 해석상의 어려움이 있으므로 초기 모델에는 사용하지 않는다.

---

## 6. 데이터 정규화

로그마다 컬럼명이 다르므로 공통 컬럼으로 변환한다.

| 공통 필드 | 설명 |
|---|---|
| `timestamp` | 이벤트 발생 일시 |
| `date` | 분석 기준 날짜 |
| `user_id` | 사용자 ID |
| `pc_id` | 사용 PC |
| `event_type` | logon, file, device 등 로그 종류 |
| `action` | logon, logoff, connect, disconnect, file 활동 등 |
| `source_file` | 원본 로그 파일명 |

시간은 `YYYY-MM-DD HH:MM:SS` 형식으로 통일하고 사용자 ID와 PC ID는 문자열로 처리한다. 중복 행, 결측치, 로그 기간과 사용자 연결 여부를 확인한 뒤 분석용 데이터셋을 만든다.

---

## 7. 분석 단위와 Feature

### 7.1 기본 분석 단위

**사용자 × 날짜**를 한 행으로 구성한다.

| user_id | date | login_count | after_hours_logon | unique_pc | file_count | usb_count |
|---|---|---:|---:|---:|---:|---:|
| U001 | 2026-09-14 | 4 | 1 | 2 | 152 | 1 |

### 7.2 1차 Feature 후보

| 구분 | Feature | 의미 |
|---|---|---|
| 로그인 | `login_count` | 하루 로그인 횟수 |
| 로그인 | `after_hours_logon_count` | 업무시간 외 로그인 횟수 |
| 로그인 | `weekend_logon` | 주말 로그인 여부 |
| PC | `unique_pc_count` | 하루 동안 사용한 PC 수 |
| PC | `new_pc_flag` | 과거에 사용하지 않은 PC 여부 |
| 파일 | `file_count` | 하루 파일 활동 횟수 |
| 파일 | `file_count_ratio` | 개인 평균 대비 파일 활동 배수 |
| 파일 | `after_hours_file_count` | 업무시간 외 파일 활동 수 |
| USB | `usb_connect_count` | USB 연결 횟수 |
| 연계 | `files_near_usb` | USB 연결 전후 일정 시간의 파일 활동 수 |

처음부터 Feature를 과도하게 늘리지 않고, 실제 컬럼으로 계산 가능한 8~10개를 우선 구현한다.

---

## 8. 정상 행동 기준선

사용자별 과거 일정 기간의 행동으로 다음 값을 계산한다.

- 평균 및 중앙값
- 표준편차
- 주 사용 시간대
- 주 사용 PC 목록
- 평균 파일 활동량
- 평균 USB 사용량

데이터가 충분하지 않은 사용자는 전체 또는 동일 부서 사용자의 중앙값을 보조 기준으로 사용할 수 있다. 기준선은 분석 대상 시점보다 이전 데이터로만 생성해 미래 정보가 포함되지 않도록 한다.

---

## 9. 이상탐지 방법

### 9.1 Isolation Forest

사용자×날짜 Feature를 입력해 각 행의 이상 점수를 계산한다. 모델의 역할은 여러 행동 변수를 함께 보았을 때 정상 데이터와 멀리 떨어진 사례를 찾는 것이다.

주요 출력:

- `anomaly_score`: 모델 이상 점수
- `is_anomaly`: 설정한 기준에 따른 이상 여부
- 위험 사용자 상위 목록

### 9.2 규칙 기반 탐지

모델 결과의 설명을 위해 간단한 규칙을 함께 적용한다.

| 규칙 | 예시 조건 | 점수 예시 |
|---|---|---:|
| 업무시간 외 로그인 | 평일 20시 이후 또는 7시 이전 | +10 |
| 신규 PC 사용 | 과거 사용 이력이 없는 PC | +15 |
| 파일 활동 급증 | 개인 기준선의 3배 초과 | +20 |
| USB 연결 | 해당 일 USB 연결 발생 | +15 |
| USB 주변 대량 파일 활동 | 연결 전후 30분 내 파일 활동 급증 | +30 |

정확한 시간, 배수, 점수는 데이터 분포를 확인한 뒤 조정한다.

### 9.3 최종 위험 점수

초기안은 다음처럼 단순하게 구성한다.

```text
최종 위험 점수 = 모델 이상 점수 60% + 규칙 점수 40%
```

가중치는 절대적인 정답이 아니라 시연과 비교를 위한 초기값이다. 최종 보고서에는 설정 근거와 한계를 함께 기록한다.

---

## 10. 탐지 결과와 설명 방식

최종 결과표에는 다음 항목을 포함한다.

| 순위 | 사용자 | 날짜 | 위험점수 | 위험등급 | 주요 판단 근거 |
|---:|---|---|---:|---|---|
| 1 | U001 | 2026-09-14 | 87 | 높음 | 야간 로그인, 신규 PC, 파일 4.2배 증가, USB 연결 |

상위 위험 사용자를 선택하면 해당 날짜의 로그를 시간순으로 보여준다.

```text
21:48 신규 PC 로그인
22:03 파일 활동 시작
22:10 USB 연결
22:12~22:31 파일 활동 73건
22:35 USB 해제
```

본 프로젝트에서 공격 흐름은 복잡한 그래프 학습이 아니라 **동일 사용자의 로그를 시간순으로 연결한 타임라인**으로 구현한다.

---

## 11. 결과 화면 구성

Streamlit 또는 Power BI 중 팀 역량에 맞는 도구 하나를 선택한다.

### 필수 화면

1. 전체 로그 및 분석 기간 요약
2. 위험 사용자 Top 10
3. 사용자별 위험 점수와 판단 근거
4. 선택 사용자의 행동 타임라인
5. 정상·이상 사용자의 주요 Feature 비교
6. 모델 성능 또는 검증 결과

### 선택 화면

- 날짜별 이상 사용자 수
- 부서별 위험 사용자 분포
- 위험 점수 기준 변경 기능
- 이메일·웹 로그 확장 결과

---

## 12. 성능 검증 계획

### 12.1 공격 라벨이 있는 경우

| 지표 | 확인 내용 |
|---|---|
| Recall | 실제 이상 사용자를 얼마나 탐지했는가 |
| Precision | 탐지 사용자 중 실제 이상 사용자의 비율 |
| F1-score | Recall과 Precision의 균형 |
| PR-AUC | 불균형 데이터에서의 전반적인 탐지 성능 |
| Top-K Recall | 위험 순위 상위 K명에 실제 이상 사용자가 포함되는 비율 |

### 12.2 공격 라벨이 없는 경우

- 야간 로그인, 대량 파일 접근, USB 연결을 조합한 합성 이상 시나리오 생성
- 합성 이상 데이터 탐지 여부 확인
- 위험 점수 상위 사용자의 원본 로그 사례 분석
- Rule Only와 Rule+Isolation Forest 결과 비교
- 임계값에 따른 하루 경보 건수 비교

### 12.3 비교 실험

| 실험 | 구성 | 목적 |
|---|---|---|
| A | 규칙 기반 탐지만 사용 | 기본 탐지 수준 확인 |
| B | Isolation Forest만 사용 | 비지도 모델의 탐지 특성 확인 |
| C | 규칙 + Isolation Forest | 탐지력과 설명 가능성의 결합 확인 |

---

## 13. 프로젝트 성공 기준

### 필수 완료 기준

1. 세 종류의 핵심 로그를 공통 형식으로 정규화한다.
2. 사용자×날짜 분석 테이블을 생성한다.
3. 8개 이상의 행동 Feature를 계산한다.
4. 사용자별 정상 행동 기준선을 생성한다.
5. Isolation Forest로 이상 점수를 산출한다.
6. 최소 4개의 설명 규칙을 구현한다.
7. 최종 위험 사용자 순위와 판단 근거를 출력한다.
8. 상위 위험 사용자의 행동 타임라인을 제공한다.
9. 라벨 또는 합성 이상 시나리오로 결과를 검증한다.
10. 전체 과정을 한 번에 재실행할 수 있도록 정리한다.

### 선택 완료 기준

- LDAP 기반 부서 비교
- 이메일 또는 웹 로그 추가
- 대시보드 필터 기능
- 사용자가 임계값을 변경하는 기능

---

## 14. 제외 범위

3주 내 완성을 위해 다음은 기본 범위에서 제외한다.

- 실시간 스트리밍 처리
- 외부 IP 기반 계정탈취 탐지
- 복잡한 다중 Entity 공격 그래프
- 자동 Incident 병합
- 공격 단계 자동 분류 모델
- DeepCASE, RCF, Transformer 학습
- 모든 MITRE ATT&CK 기법 매핑
- FastAPI 및 외부 시스템 연동
- 자동 계정·USB·IP 차단
- 상용 SIEM 수준의 기능과 성능

---

## 15. 역할 분담안

| 담당 | 주요 역할 | 핵심 산출물 |
|---|---|---|
| A | 데이터 구조 분석·정규화 | 정규화 데이터, 데이터 사전 |
| B | Feature·기준선 생성 | 사용자×날짜 Feature 테이블 |
| C | Isolation Forest·규칙·평가 | 이상 점수, 위험 점수, 성능표 |
| D | 시각화·통합·문서화 | 대시보드, 타임라인, 실행 문서 |

각 담당자는 별도로 작업하되 공통 키인 `timestamp`, `user_id`, `pc_id`와 최종 Feature 컬럼을 초기에 합의한다.

---

## 16. 15일 개발 일정

| 기간 | 주요 업무 | 완료 기준 |
|---|---|---|
| Day 1~2 | 로그 컬럼·기간·결측·중복·연결키 확인 | 데이터 구조와 분석 범위 확정 |
| Day 3~4 | 로그 정규화 및 통합 | 공통 Event 테이블 생성 |
| Day 5~6 | 사용자×날짜 Feature 생성 | 8개 이상 Feature 계산 |
| Day 7 | 사용자별 기준선 생성 | 개인 평균·주 사용 PC 등 산출 |
| Day 8~9 | Isolation Forest 적용 | 이상 점수와 상위 사용자 출력 |
| Day 10 | 위험 규칙과 최종 점수 결합 | 판단 근거 자동 생성 |
| Day 11~12 | 사용자 타임라인·대시보드 구현 | 결과 화면 완성 |
| Day 13 | 성능평가 및 임계값 비교 | 평가표 생성 |
| Day 14 | 오류 수정·전체 재실행 | 처음부터 끝까지 실행 성공 |
| Day 15 | 발표 자료·시연 준비 | 최종 데모 완성 |

### 개발 점검 기준

- **Day 4:** 원본 CSV가 하나의 공통 구조로 정리되는가?
- **Day 9:** 사용자별 이상 점수가 정상적으로 계산되는가?
- **Day 12:** 상위 위험 사용자의 근거와 타임라인이 보이는가?
- **Day 15:** 데이터 입력부터 결과 화면까지 재현 가능한가?

---

## 17. 예상 리스크와 대응

| 리스크 | 대응 방향 |
|---|---|
| 파일별 사용자 키가 일치하지 않음 | ID 형식 정리 후 조인 성공률 확인 |
| 사용자별 데이터가 부족함 | 전체 또는 부서 중앙값을 보조 기준으로 사용 |
| 공격 라벨이 없음 | 합성 이상 시나리오와 상위 사례 분석으로 검증 |
| 정상 USB 사용이 이상으로 탐지됨 | USB 단독이 아닌 파일 급증과의 결합 여부 확인 |
| 이상 사용자가 너무 많이 탐지됨 | contamination과 위험 임계값을 비교 조정 |
| 팀원 작업 통합이 지연됨 | 공통 컬럼과 파일 출력 형식을 Day 2에 확정 |
| 모델 결과를 설명하기 어려움 | 규칙 기반 판단 근거를 함께 출력 |

---

## 18. 최종 산출물

| 산출물 | 내용 |
|---|---|
| 정규화 로그 | 공통 컬럼으로 변환한 로그 데이터 |
| Feature 데이터셋 | 사용자×날짜 행동 지표 |
| 기준선 데이터 | 사용자별 정상 행동 통계 |
| 이상탐지 모델 | Isolation Forest 모델 및 설정값 |
| 위험 사용자 결과 | 위험점수, 등급, 판단 근거 |
| 사용자 타임라인 | 상위 위험 사용자의 시간순 행동 |
| 대시보드 | 위험 사용자 및 근거 시각화 |
| 평가 결과 | 모델·규칙 비교 및 성능지표 |
| 최종 보고서 | 분석 과정, 결과, 한계, 확장 방향 |

---

## 19. 향후 확장 방향

기본 PoC가 완료된 이후 다음 기능을 단계적으로 추가할 수 있다.

1. `email.csv`, `http.csv`를 이용한 외부 전송 정황 분석
2. LDAP 부서·직무별 동료 집단 기준선 적용
3. 시간 단위를 일 단위에서 1시간 단위로 세분화
4. OpenSearch 또는 SIEM 환경과 연동
5. 공격 단계 분류 및 Incident 자동 구성
6. 시계열·그래프 기반 고급 모델 적용

---

## 20. 프로젝트 핵심 정의

> 본 프로젝트는 로그인·파일·USB 로그를 사용자별로 정규화하고, 개인의 평소 행동 기준선과 Isolation Forest를 이용해 이상 사용자를 탐지한 뒤, 규칙 기반 위험 점수와 시간순 행동 타임라인으로 판단 근거를 제공하고 탐지 결과를 검증하는 설명 가능한 보안 이상탐지 PoC이다.

### 회의용 최종 제안 문장

> 기존 기획안의 실시간 다중 공격 탐지 기능은 향후 확장으로 두고, 3주 동안 실제 구현 가능한 범위인 로그인·파일·USB 로그 기반 이상 사용자 탐지에 집중합니다. 사용자별 하루 행동을 Feature로 만들고 Isolation Forest로 이상도를 계산하며, 야간 로그인·신규 PC·파일 급증·USB 연결 규칙을 결합해 왜 위험한지 설명합니다. 최종적으로 위험 사용자 순위, 행동 타임라인, 성능평가 결과를 제공하는 것을 목표로 합니다.

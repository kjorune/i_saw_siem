# 보안 로그 샘플 데이터셋 — 파일·컬럼 정의서

작성일: 2026-09-14  
분석 목적: 보안 회사의 데이터분석 프로젝트에서 사용할 데이터 구조와 컬럼 의미 파악

## 1. 분석 범위와 해석 기준

`device.csv`, `email.csv`, `file.csv`, `http.csv`, `logon.csv`, `psychometric.csv`, `LDAP/2009-12.csv`의 **7개 파일을 분석했다.

CSV 파서로 전체 행을 읽어 컬럼 수, 빈 값, 이벤트 ID 중복, 날짜 범위·파싱, 활동 종류, 주요 숫자 범위 및 사용자 연결 관계를 확인했다. 내용 필드의 의미는 헤더와 대표 값의 구조를 함께 검토했다.

이 데이터는 **사용자 활동 로그 5종과 직원 속성 데이터 2종**으로 구성된다.

## 2. 파일별 개요

| 파일명 | 데이터 성격 / 한 행의 의미 | 데이터 행 수 | 컬럼 수 | 고유 사용자 수 | 확인된 기간 또는 기준 |
|---|---|---:|---:|---:|---|
| `device.csv` | 이동식 장치 연결·해제 이벤트 1건 | 405,380 | 5 | 265 | 2010-01-02 07:21:06 ~ 2011-05-16 23:22:34 |
| `email.csv` | 사용자·PC에 연결된 이메일 이벤트 1건 | 1,314,400 | 11 | 1,000 | 2010-01-02 07:11:45 ~ 2011-05-16 21:16:26 |
| `file.csv` | 사용자·PC에서 발생한 파일 관련 이벤트 1건 | 445,581 | 6 | 264 | 2010-01-02 07:23:14 ~ 2011-05-16 23:22:34 |
| `http.csv` | 사용자·PC의 웹 URL 접근 이벤트 1건 | 1,422,900 | 6 | 1,000 | 2010-01-02 07:14:28 ~ 2011-05-16 21:07:23 |
| `logon.csv` | PC 로그인·로그오프 이벤트 1건 | 854,859 | 5 | 1,000 | 2010-01-02 06:49:00 ~ 2011-05-17 06:43:35 |
| `psychometric.csv` | 직원 1명의 성격 특성 점수 | 1,000 | 7 | 1,000 | 측정일 컬럼 없음 |
| `LDAP/2009-12.csv` | 직원 1명의 계정·조직 정보 | 1,000 | 9 | 1,000 | 파일명상 2009년 12월 스냅샷 |

**기간은 제공된 파일에 존재하는 이벤트의 최솟값·최댓값**이다. 이 기간의 모든 실제 행위가 빠짐없이 수집되었다는 뜻은 아니다. 특히 일부 로그에 사용자가 없다는 사실만으로 그 사용자가 해당 행위를 전혀 하지 않았다고 단정할 수 없다.

## 3. 활동 로그의 공통 컬럼

아래 4개 컬럼은 `device.csv`, `email.csv`, `file.csv`, `http.csv`, `logon.csv`에 공통으로 존재한다.

| 컬럼명 | 권장 자료형 | 컬럼 의미 | 확인 사항 및 분석 시 주의점 |
|---|---|---|---|
| `id` | 문자열 | 개별 로그 이벤트를 식별하는 ID | 중괄호와 하이픈을 포함하는 식별자 형태. 각 파일 내부의 중복은 0건이다. 표준 UUID 형식으로 단정하지 않는다. 파일 간 연결 키로 사용하지 말고, 통합 로그에서는 `source_file`과 함께 관리한다. 파일 간 ID 중복 여부는 이번 검사에 포함하지 않았다. |
| `date` | 날짜·시간 | 이벤트 발생 시각으로 해석 | 원본은 `MM/DD/YYYY HH:mm:ss`. 예: `01/02/2010`은 2010년 1월 2일. 시간대·UTC 오프셋 정보가 없으므로 한국 시간이나 UTC로 임의 해석하지 않는다. |
| `user` | 문자열 | 이벤트에 연결된 사용자 계정 ID | `ABC0001` 같은 형태. LDAP 및 심리측정 데이터의 `user_id`와 연결된다. 계정 ID에 포함된 숫자의 앞자리 0을 보존한다. 이메일의 경우 `from` 주소와 동일한 개념으로 취급하지 않는다. |
| `pc` | 문자열 | 이벤트가 발생한 PC·단말 식별자 | `PC-0001` 같은 형태. IP 주소나 장치 소유자 ID가 아니다. 사용자와 PC가 항상 일대일이라고 가정하지 않는다. |

## 4. 파일별 컬럼 정의

### 4.1. `device.csv` — 이동식 장치 연결·해제 로그

**의미:** 사용자가 PC에 이동식 장치를 연결하거나 해제한 기록으로 해석한다. USB 저장장치 사용 패턴을 살펴보는 데 적합하지만, 장치 종류나 장치 고유 ID 자체는 없다.

| 컬럼명 | 권장 자료형 | 컬럼별 의미 | 값의 형태 / 확인 사항 |
|---|---|---|---|
| `id` | 문자열 | 장치 연결·해제 이벤트 ID | 파일 내부 중복 없음 |
| `date` | 날짜·시간 | 연결 또는 해제가 기록된 시각 | `MM/DD/YYYY HH:mm:ss` |
| `user` | 문자열 | 해당 장치 이벤트에 연결된 사용자 | 고유 사용자 265명 |
| `pc` | 문자열 | 장치가 연결·해제된 PC | 고유 PC 971대 |
| `activity` | 범주형 문자열 | 장치에 대해 수행된 행위 | `Connect`: 연결, 203,339건 / `Disconnect`: 해제, 202,041건 |

**분석 시 활용:** 사용자별 연결 빈도, 근무시간 외 사용, 평소와 다른 PC에서의 연결, 파일 이벤트와의 시간적 연관성.

**해석상 주의:** 장치 일련번호·용량·전송량은 없다. `Connect`는 데이터 복사 완료를 뜻하지 않는다. 연결과 해제의 총건수가 다르므로 두 행을 기계적으로 짝지어 사용시간을 계산해서는 안 된다. `user`·`pc`·시간 순서를 기준으로 누락·중첩 상황을 처리해야 한다.

### 4.2. `email.csv` — 이메일 활동 로그

**의미:** 이메일 주소 관계, 크기, 첨부 개수, 내용 특성을 기록한 데이터다. 현재 스키마에는 발송·수신·열람을 구분하는 `activity` 컬럼이 없으므로 각 행의 정확한 수집 방향은 확인이 필요하다.

| 컬럼명 | 권장 자료형 | 컬럼별 의미 | 값의 형태 / 확인 사항 |
|---|---|---|---|
| `id` | 문자열 | 이메일 이벤트 ID | 파일 내부 중복 없음. 메일 서버의 RFC Message-ID와 동일하다고 단정할 수 없음 |
| `date` | 날짜·시간 | 이메일 이벤트가 기록된 시각 | 발송·수신·수집 중 어느 시각인지는 추가 명세 필요 |
| `user` | 문자열 | 이메일 활동에 연결된 사용자 계정 | 고유 사용자 1,000명. `from`과 별개의 필드 |
| `pc` | 문자열 | 해당 이메일 활동이 기록된 PC | 고유 PC 1,000대 |
| `to` | 문자열 → 주소 목록 | 주 수신자 이메일 주소 | 여러 주소는 `;`로 구분. 빈 값 없음 |
| `cc` | 문자열 → 주소 목록, 빈 값 허용 | 참조 수신자 이메일 주소. CC는 Carbon Copy | 여러 주소는 `;`로 구분. 빈 값 808,452건 |
| `bcc` | 문자열 → 주소 목록, 빈 값 허용 | 숨은 참조 수신자 이메일 주소. BCC는 Blind Carbon Copy | 여러 주소는 `;`로 구분. 빈 값 1,105,790건 |
| `from` | 문자열 | 이메일의 발신 주소 | 사내 도메인과 외부 도메인이 모두 관찰됨. 세미콜론으로 구분된 다중 주소는 관찰되지 않음 |
| `size` | 정수 | 이메일 메시지의 크기를 나타내는 수치로 해석 | 6,241~129,659. 바이트 단위로 추정하되, 정확한 단위와 첨부 포함 여부는 제공 파일만으로 확정 불가 |
| `attachments` | 정수 | 첨부파일 개수로 해석 | 0~9. `0`은 첨부 없음으로 해석하며 결측치와 다름. 파일명·파일 크기 목록이 아님 |
| `content` | 문자열 | 이메일 내용의 텍스트·주제 키워드 표현 | 대표 행은 공백으로 구분된 단어 나열 형태. 원문 본문 전체, 제목 또는 제목·본문의 결합이라고 단정할 수 없음 |

**확인한 데이터 특성**

- 빈 `cc`는 61.51%, 빈 `bcc`는 84.13%다. 참조·숨은 참조가 없는 경우로 해석할 수 있으므로, 빈 값을 일괄 오류로 처리하지 않는다.
- 복수 주소가 있는 행은 `to` 529,534건, `cc` 135,661건, `bcc` 11,583건이다. 수신자 수·외부 도메인 여부는 CSV 행을 먼저 파싱한 뒤 각 필드를 `;`로 분리해서 계산한다.
- `from`과 해당 `user`의 LDAP 등록 이메일을 대소문자 구분 없이 비교하면, **819,522건은 일치하고 494,878건(37.65%)은 다르다.** 따라서 `user`를 언제나 `from` 주소의 소유자로 간주하면 안 된다. 이 불일치만으로 수신 메일, 개인 메일 사용 또는 위조라고 단정할 수는 없다.
- `attachments=0`은 1,032,820건이다. 메시지 크기를 첨부파일 크기로 해석하거나, `size`와 첨부 개수를 같은 의미의 변수로 취급하지 않는다.

**분석 시 활용:** 수신자 수, 사내·외 주소 관계, 숨은 참조 사용 패턴, 첨부 개수, 사용자별 메시지 크기 변화. 발송·수신 방향을 검증하기 전에는 외부 주소가 포함된 행 전체를 ‘외부로 반출한 메일’로 집계하지 않는다.

### 4.3. `file.csv` — 파일 관련 행위 로그

**의미:** 사용자와 PC를 기준으로 파일명 및 내용 표현이 기록된 이벤트다. 

| 컬럼명 | 권장 자료형 | 컬럼별 의미 | 값의 형태 / 확인 사항 |
|---|---|---|---|
| `id` | 문자열 | 파일 관련 이벤트 ID | 파일 내부 중복 없음. 파일 자체의 고유 ID나 해시가 아님 |
| `date` | 날짜·시간 | 파일 이벤트 발생 시각 | 파일 생성·최종 수정 시각을 뜻하는 컬럼은 아님 |
| `user` | 문자열 | 파일 이벤트를 수행한 것으로 해석되는 사용자 | 고유 사용자 264명 |
| `pc` | 문자열 | 파일 이벤트가 발생한 PC | 고유 PC 956대 |
| `filename` | 문자열 | 이벤트 대상 파일명 | 대표 값은 `SAMPLE01.doc`처럼 이름과 확장자로 구성. 파일명만으로 동일 파일 여부를 확정할 수 없음 |
| `content` | 문자열 | 파일 형식 관련 헤더 표현과 텍스트·주제 키워드 | 대표 행에서 하이픈으로 구분된 16진수 접두부 뒤에 단어들이 이어짐. 원본 바이너리 전체나 해시로 해석하지 않음 |

**확인한 데이터 특성**

| 확장자 | 이벤트 건수 |
|---|---:|
| `.doc` | 285,897 |
| `.pdf` | 87,953 |
| `.txt` | 23,033 |
| `.jpg` | 22,895 |
| `.zip` | 22,829 |
| `.exe` | 2,974 |

위 수치는 고유 파일 개수가 아니라 **해당 확장자가 기록된 이벤트 수**다. 

**분석 시 활용:** 파일 이벤트 빈도, 확장자별 사용 패턴, 장치 연결 전후의 파일 활동 집중, 사용자별 파일 내용 특성 변화.

**해석상 주의:** 현재 파일에는 `activity`, 파일 크기, 원본·대상 경로, 장치 ID, 이동식 매체 방향 플래그가 없다. 열기·쓰기·복사·삭제를 개별 행에 임의로 부여하거나, 건수를 데이터 반출 바이트 수로 환산하면 안 된다.

### 4.4. `http.csv` — 웹 접근 로그

**의미:** 사용자의 웹 URL 접근과 관련된 이벤트다. 네트워크 패킷 전체나 HTTP 요청·응답 전문을 담은 파일은 아니다.

| 컬럼명 | 권장 자료형 | 컬럼별 의미 | 값의 형태 / 확인 사항 |
|---|---|---|---|
| `id` | 문자열 | 웹 접근 이벤트 ID | 파일 내부 중복 없음 |
| `date` | 날짜·시간 | 웹 접근이 기록된 시각 | 접속 종료 시각이나 체류시간 정보는 없음 |
| `user` | 문자열 | 웹 접근에 연결된 사용자 | 고유 사용자 1,000명 |
| `pc` | 문자열 | 웹 접근이 발생한 PC | 고유 PC 1,000대 |
| `url` | 문자열 | 접근한 웹 주소 | 스킴·호스트·경로 등으로 분해 가능. 제공된 1,422,900행의 스킴은 모두 `http` |
| `content` | 문자열 | 접근 대상 웹 콘텐츠의 텍스트·주제 키워드 표현으로 해석 | 대표 행은 공백으로 구분된 단어 나열. HTML 원문, 업로드 본문 또는 다운로드 파일 전체라고 단정할 수 없음 |

**분석 시 활용:** 도메인별 접근 빈도, 사용자별 새로운 도메인 접근, 시간대별 패턴, URL 경로와 내용 키워드 변화.

**해석상 주의:** HTTP 메서드, 상태 코드, 전송량, 목적지 IP, 업로드·다운로드 구분이 없다. URL 접근만으로 정보 업로드나 유출을 확정할 수 없다. 모든 스킴이 `http`라는 결과도 이 샘플의 관찰값이며, 원래 환경에 HTTPS 사용이 없었다는 증거는 아니다.

### 4.5. `logon.csv` — 로그인·로그오프 로그

**의미:** 사용자가 PC에 로그인하거나 로그오프한 이벤트를 기록한다. 한 행은 하나의 이벤트이며, 로그인부터 로그오프까지의 완성된 세션이 아니다.

| 컬럼명 | 권장 자료형 | 컬럼별 의미 | 값의 형태 / 확인 사항 |
|---|---|---|---|
| `id` | 문자열 | 로그인·로그오프 이벤트 ID | 파일 내부 중복 없음 |
| `date` | 날짜·시간 | 로그인 또는 로그오프 시각 | `MM/DD/YYYY HH:mm:ss` |
| `user` | 문자열 | 로그인·로그오프한 사용자 계정 | 고유 사용자 1,000명 |
| `pc` | 문자열 | 로그인·로그오프 대상 PC | 고유 PC 1,003대 |
| `activity` | 범주형 문자열 | 세션 관련 행위 종류 | `Logon`: 로그인, 470,591건 / `Logoff`: 로그오프, 384,268건 |

**분석 시 활용:** 사용자별 첫 로그인·마지막 로그오프, 시간대별 활동, 평소와 다른 PC 사용, 세션 추정.

**해석상 주의:** 성공실패 상태, 원격·로컬 구분, 인증 방식, 세션 ID는 없다. 로그인 실패 탐지용 데이터라고 볼 수 없다. `Logon`과 `Logoff`의 건수가 다르므로, 누락·연속 로그인·날짜를 넘는 세션을 고려해야 하며 단순히 인접한 두 행을 짝지으면 안 된다.

### 4.6. `psychometric.csv` — 직원 성격 특성 데이터

**의미:** 직원별 성격 5요인인 OCEAN 점수를 제공하는 속성 데이터다. 

| 컬럼명 | 권장 자료형 | 컬럼별 의미 | 확인된 값 / 해석상 주의 |
|---|---|---|---|
| `employee_name` | 문자열 | 직원 이름 | 1,000개의 고유 이름. 연결에는 이름보다 `user_id` 사용 |
| `user_id` | 문자열 | 직원 사용자 계정 ID | 1,000개의 고유 ID. 활동 로그의 `user`, LDAP의 `user_id`와 연결 |
| `O` | 정수 | **Openness: 개방성.** 새로운 경험·아이디어에 대한 수용성, 호기심 등 | 관찰 범위 10~50 |
| `C` | 정수 | **Conscientiousness: 성실성.** 계획성·책임감·자기관리 등 | 관찰 범위 10~50 |
| `E` | 정수 | **Extraversion: 외향성.** 사교성·활동성·대인 교류 성향 등 | 관찰 범위 10~50 |
| `A` | 정수 | **Agreeableness: 우호성 / 친화성.** 협조·배려·타인에 대한 신뢰 성향 등 | 관찰 범위 10~50 |
| `N` | 정수 | **Neuroticism: 신경성.** 스트레스·부정적 정서에 대한 민감성 등 | 관찰 범위 14~49. 정서적 안정성과 같은 방향의 점수로 해석하지 않음 |

**해석상 주의:** 위 범위는 실제 관찰된 최솟값·최댓값이며, 검사에서 허용하는 이론적 범위가 확인된 것은 아니다. 점수를 백분율, 임상 진단, 악성 사용자 라벨 또는 보안 위험 점수로 해석하지 않는다. 보안 분석에서는 행동 로그에 덧붙이는 사용자 속성으로 검토할 수 있으나, 이 점수만으로 악성 행위를 판정할 근거는 없다.

### 4.7. `LDAP/2009-12.csv` — 직원 계정·조직 정보

**의미:** LDAP는 Lightweight Directory Access Protocol의 약자다. **직원 디렉터리·조직 정보 테이블**이다. 파일은 2009년 12월 기준 스냅샷으로 해석한다.

| 컬럼명 | 권장 자료형 | 컬럼별 의미 | 확인 사항 및 주의점 |
|---|---|---|---|
| `employee_name` | 문자열 | 직원 이름 | 고유 값 1,000개. 심리측정 데이터와 같은 `user_id`의 이름이 모두 일치 |
| `user_id` | 문자열 | 직원 사용자 계정 ID | 고유 값 1,000개. 이 스냅샷의 기본키로 사용 가능 |
| `email` | 문자열 | 직원의 등록 이메일 주소 | 고유 값 1,000개. 모두 `dtaa.com` 도메인 |
| `role` | 범주형 문자열 | 직무·직책 | 42종. 예: `SoftwareEngineer`, `ITAdmin`. 시스템 접근 권한이나 IAM 역할 목록으로 단정하지 않음 |
| `business_unit` | 범주형 문자열 | 상위 사업 조직 코드로 해석 | 모든 직원의 값이 `1`. 수량·점수가 아니며, 사업 조직 명칭은 제공되지 않음 |
| `functional_unit` | 범주형 문자열, 빈 값 허용 | 기능별 조직·본부 구분으로 해석 | `번호 - 조직명` 형태. 비어 있지 않은 값 6종, 빈 값 2건 |
| `department` | 범주형 문자열, 빈 값 허용 | 부서 구분 | `번호 - 부서명` 형태. 비어 있지 않은 값 22종, 빈 값 14건 |
| `team` | 범주형 문자열, 빈 값 허용 | 팀 구분 | `번호 - 팀명` 형태. 비어 있지 않은 값 38종, 빈 값 127건 |
| `supervisor` | 문자열, 빈 값 허용 | 직속 상사 이름으로 해석 | 사용자 ID가 아니라 이름. 비어 있지 않은 상사 이름 67개, 빈 값 1건 |

**분석 시 활용:** 직무·부서·팀이 유사한 직원 사이의 행위 비교, 조직 단위 집계, 상사 관계 분석, 등록 이메일을 이용한 사내 주소 매핑.

**해석상 주의**

- `business_unit → functional_unit → department → team`은 상위 사업 조직에서 하위 팀으로 이어지는 계층으로 해석할 수 있다. 정확한 조직 체계는 별도 명세로 확인해야 한다.
- `번호 - 이름`에서 번호만 떼어 전사적으로 유일한 코드로 간주하지 않는다. 조직별 지역 코드일 수 있으므로 원문 전체와 상위 조직을 보존한다.
- 일부 계층이 비어 있는 원인은 미배정·상위 직책·수집 누락 등으로 나뉠 수 있지만, 현재 파일만으로 어느 원인인지 확정할 수 없다.
- 비어 있지 않은 모든 `supervisor` 값은 이 스냅샷의 `employee_name`과 매칭되며, 직원 이름 중복은 없다. 따라서 현재 파일에서는 이름으로 상사를 찾고 그 상사의 `user_id`를 가져오는 방식으로 관계를 구성할 수 있다.
- 이 파일에는 2010~2011년의 인사 변동이 직접 들어 있지 않다. 미래 로그에 결합하더라도 그 시점의 실제 부서·직책이라는 보장은 없다.

## 5. 파일 간 연결 관계

| 연결 대상 | 연결 키 / 방식 | 이번 검사 결과 | 적용 시 주의 |
|---|---|---|---|
| 활동 로그 ↔ LDAP | 로그의 `user` = LDAP의 `user_id` | 5개 활동 로그의 모든 사용자 ID가 LDAP에 존재 | 로그 여러 행 : 직원 1행 관계. `2009-12` 기준 속성임을 보존 |
| 활동 로그 ↔ 심리측정 | 로그의 `user` = 심리측정의 `user_id` | 5개 활동 로그의 모든 사용자 ID가 심리측정 파일에 존재 | 직원 단위 속성이므로 이벤트마다 복제되더라도 독립 관측값이 늘어난 것은 아님 |
| LDAP ↔ 심리측정 | `user_id` | 두 파일의 사용자 1,000명이 모두 일치하며 이름도 일치 | 이번 파일에서는 일대일 연결 가능 |
| 이메일 ↔ LDAP 등록 주소 | `from`, 분리한 `to`·`cc`·`bcc` ↔ LDAP의 `email` | LDAP 등록 주소는 모두 `dtaa.com` | 복수 주소는 분리 후 연결. 전체 수신자 주소의 등록 여부는 이번 검사 범위에 포함하지 않음 |
| LDAP 직원 ↔ 상사 | `supervisor` = 다른 행의 `employee_name` | 비어 있지 않은 상사 이름은 모두 매칭 | 매칭 후 상사의 `user_id`로 관계를 저장하는 편이 안정적 |
| 활동 로그 ↔ 다른 활동 로그 | `user`·`pc`·시간 순서 또는 시간 구간 | 해당 필드가 모든 활동 로그에 존재 | `id`로 서로 다른 종류의 이벤트를 조인하지 않음. 연결·세션 매칭 자체는 이번 분석에서 수행하지 않음 |

예를 들어 ‘로그인 → 장치 연결 → 파일 이벤트 → 장치 해제’의 흐름을 살펴보려면 동일 사용자의 동일 PC에서 발생한 이벤트를 시간 순서로 정렬한다. 시간적 근접성은 연관성을 살펴보는 근거이며, 동일 세션·동일 장치·유출 행위가 증명된 것은 아니다.

## 6. 데이터 품질 확인 결과

| 확인 항목 | 결과 |
|---|---|
| CSV 구조 | 7개 파일 모두 헤더와 필드 수가 다른 데이터 행 0건 |
| 이벤트 ID 중복 | 5개 활동 로그 각각에서 `id` 중복 0건 |
| 날짜 파싱 | 5개 활동 로그에서 지정 형식으로 변환할 수 없는 값 0건 |
| 날짜 정렬 | 각 활동 로그에서 앞 행보다 시간이 이른 행 0건. 같은 시각의 이벤트는 허용 |
| 숫자 변환 | `size`, `attachments`, `O`, `C`, `E`, `A`, `N`의 정수 변환 실패 0건 |
| 활동 로그의 빈 값 | 이메일의 `cc`, `bcc`에서만 관찰. 나머지 활동 로그 컬럼의 빈 값 0건 |
| 심리측정 빈 값 | 모든 컬럼에서 0건 |
| LDAP 빈 값 | `functional_unit` 2건, `department` 14건, `team` 127건, `supervisor` 1건 |
| 직원 연결 | 활동 로그의 사용자 ID가 LDAP·심리측정에 모두 존재 |
| 악성 여부 컬럼 | 분석한 7개 파일에 악성 여부를 나타내는 명시적 정답 컬럼 없음 |

빈 값은 비어 있거나 공백만 있는 문자열을 기준으로 계산했다. 문자열 `NULL` 등의 별도 결측 표기 탐지나 전체 행 중복 검사는 수행하지 않았다. ID 중복 검사는 파일별로 수행했으며, 파일 간 전역 유일성 검사가 아니다.

## 7. 프로젝트 적용 시 먼저 정할 사항

1. **로그 시간대와 근무시간 기준:** `date`에는 시간대가 없고, 근무표·휴일 달력도 없다. 특정 시간대를 곧바로 비정상 근무시간으로 지정하기 전에 분석 기준을 정한다.
2. **이메일 크기·방향과 파일 작업 의미:** `size`의 단위·첨부 포함 여부, 이메일의 발송·수신 구분, 파일 이벤트의 작업·전송 방향은 릴리스 설명서 또는 제공처의 데이터 사전으로 확인한다.
3. **조직 정보의 적용 시점:** 이번에 분석한 LDAP는 `2009-12` 파일이다. 월별 인사 변동을 반영하려면 다른 월 파일도 확인하고, 각 스냅샷의 실제 기준일·효력 범위에 따라 이벤트에 연결한다.
4. **정답 데이터와 샘플링 범위:** 이 7개 파일의 행위만으로 정상·악성 라벨을 확정할 수 없다. 지도학습이나 탐지 성능 평가에는 현재 샘플과 대응하는 정답·추출 기준이 필요하다. 


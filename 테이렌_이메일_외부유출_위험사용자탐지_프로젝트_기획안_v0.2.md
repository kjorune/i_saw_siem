# 프로젝트 기획안 수정본 (킥오프 발표)


## a. 프로젝트 주제 (프로젝트명)

### 프로젝트명

**SIEM 로그 기반 이메일 외부 유출 위험 사용자 탐지 및 위험 스코어링**

### 프로젝트 한 문장 정의

> **Teiren Cloud SIEM에 저장된 이메일 보안 로그를 활용해 사용자별 평소 이메일 행동 기준선을 만들고, 외부 수신자·첨부파일·메일 크기·BCC·시간대·신규 관계 등의 이상징후를 통계적으로 분석하여 외부 유출 위험이 높은 사용자를 점수화하고 우선순위화하는 프로젝트**

### 분석 문제 구조

> **X = 행동지표 / Y 없음 / 최종 Output = Risk Score**

- **X(입력):** 사용자별 외부메일 수, 외부 수신자 수, 첨부파일 수, 메일 크기, BCC 사용, 비정상 시간대, 신규 외부 수신자·도메인, 반복 전송 등 이메일 로그에서 만든 행동지표
- **Y(정답 라벨):** 현재 데이터에는 정상/악성 또는 유출/비유출을 나타내는 정답 라벨이 없음
- **최종 Output:** 사용자의 행동이 자신의 평소 기준선에서 얼마나 벗어났는지를 종합한 `Risk Score`, 위험순위, 점수 산정 근거

따라서 이번 프로젝트는 정답 라벨을 학습해 사용자를 분류하는 지도학습 문제가 아니라, **라벨 없이 사용자 행동의 이상 정도를 계산하고 조사 우선순위를 제공하는 비지도·통계 기반 위험 스코어링 문제**로 정의한다.

### 핵심 분석 흐름

```text
Security Log
    ↓
Email Feature
    ↓
User Baseline
    ↓
Anomaly Score
    ↓
Weighted Risk Score
    ↓
Risk User Ranking
    ↓
위험점수 산정 근거
```

### 프로젝트 범위 해석

이번 프로젝트의 목표는 실제 정보유출자를 확정하는 것이 아니다.

현재 이메일 데이터에는 악성/정상 정답 라벨이 없고, 각 이메일 행이 발송인지 수신인지 직접 표시하는 `activity` 컬럼도 없다. 따라서 결과는 다음처럼 표현하는 것이 적절하다.

> **“사용자의 평소 이메일 행동에서 벗어난 외부 전송 이상징후를 탐지하고, 외부 유출 위험이 높은 사용자를 위험점수로 우선순위화한다.”**

이때 분석 구조는 **X = 행동지표 / Y 없음 / 최종 Output = Risk Score**로 유지한다. 즉 모델의 성능을 정답 라벨에 대한 정확도로 설명하기보다, 정상·단일 이상·복합 이상·반복 행동 시나리오에서 위험점수가 의도한 방향으로 변화하는지를 검증한다.

또한 OT의 공식 표현은 **보안 로그 기반 이상탐지 및 위험 스코어링**, **사용자·자산 행동 기준선을 활용한 통계 기반 분석**이며, `UEBA`라는 용어가 OT에 직접 적혀 있는 것은 아니다. 발표에서는 프로젝트를 공식적으로 UEBA라고 단정하기보다 **사용자 행동 기준선 기반 통계적 이상탐지 및 위험 스코어링**으로 설명한다.

---

## b. 주제 선정 이유

### 1) 실제 국내 사례와 직접 연결할 수 있음

공유 문서에 정리된 NIA 사례에서는 한 직원이 **2022년부터 약 3년간 기관 이메일을 이용해 외부 민간업체 관계자에게 380회에 걸쳐 비공개 자료를 전달**한 것으로 정리되어 있다.

이 사례의 핵심 문제는 단순히 외부 이메일 한 건이 아니라 다음과 같다.

```text
내부 직원
    ↓
기관 이메일 사용
    ↓
외부 관계자에게 전달
    ↓
반복적인 자료 전송
    ↓
장기간 탐지되지 않음
```

따라서 이번 프로젝트는 다음 질문에서 출발한다.

> **“한 직원이 평소와 다른 외부 이메일 전송 행동을 반복한다면, 보안 로그를 이용해 이를 더 일찍 이상징후로 포착하고 조사 우선순위를 높일 수 있는가?”**

### 2) 현재 제공된 이메일 데이터와 분석 주제의 연결성이 높음

`email.csv`에는 다음 정보가 존재한다.

```text
date
user
pc
to
cc
bcc
from
size
attachments
content
```

이를 이용해 다음과 같은 행동지표를 만들 수 있다.

- 외부 이메일 활동 횟수
- 외부 수신자 수
- 고유 외부 수신자 수
- 신규 외부 수신자
- 신규 외부 도메인
- 외부 첨부 메일 수
- 첨부파일 개수
- 메시지 크기 변화
- BCC 사용 변화
- 비정상 시간대 이메일 활동
- 동일 외부 수신자에 대한 반복 전송

위 항목들이 모델에 입력되는 **X(행동지표)**가 된다. 현재는 이를 비교할 **Y(정상/악성 정답 라벨)**가 없으므로, 각 지표의 이상도와 조합을 이용해 최종 **Risk Score**를 산출한다.

즉 단순히 “외부 주소가 있었는가”가 아니라,

> **“이 사용자는 평소 누구에게, 얼마나 자주, 어느 정도 크기의 메일을, 몇 개의 첨부와 함께 보내는가?”**

라는 개인별 행동 패턴을 만들 수 있다.


### 3) 프로젝트 범위를 관리하기 좋음

현재 제공 데이터에는 로그인, 이메일, 웹, USB, 파일 로그 등이 있지만, 1차 범위를 이메일에 집중하면 **Baseline → 이상도 → 위험점수 → Ranking → 근거**의 전체 분석 파이프라인을 먼저 완성할 수 있다.

시간이 남는 경우에만 USB·파일 로그를 결합하는 확장 방향을 검토한다.

---

## c. 전달 받은 로그에 대한 설명 (로그 데이터 구조 등)

### 1) 핵심 분석 데이터: `email.csv`

공유 문서에 정리된 이메일 데이터의 기본 구조는 다음과 같다.

| 항목 | 내용 |
|---|---|
| 데이터 행 수 | 1,314,400건 |
| 고유 사용자 수 | 1,000명 |
| 기간 | 2010-01-02 ~ 2011-05-16 |
| 주요 컬럼 | `date`, `user`, `pc`, `to`, `cc`, `bcc`, `from`, `size`, `attachments`, `content` |

| 컬럼 | 프로젝트에서의 의미 |
|---|---|
| `date` | 이메일 이벤트 발생 시각 |
| `user` | 이메일 활동과 연결된 사용자 계정 |
| `pc` | 이벤트가 기록된 PC |
| `to` | 주 수신자 |
| `cc` | 참조 수신자 |
| `bcc` | 숨은 참조 수신자 |
| `from` | 발신 이메일 주소 |
| `size` | 이메일 메시지 크기를 나타내는 수치 |
| `attachments` | 첨부파일 개수 |
| `content` | 이메일 텍스트·주제 키워드 표현 |

### 2) 수신자 컬럼 처리 시 주의

`to`, `cc`, `bcc`에는 여러 이메일 주소가 `;`로 들어갈 수 있다.

따라서 다음과 같이 처리해야 한다.

```text
to / cc / bcc
    ↓
';' 기준 주소 분리
    ↓
개별 수신자 추출
    ↓
사내 / 외부 주소 판별
```

### 3) LDAP 데이터 활용

LDAP에는 다음 정보가 존재한다.

```text
user_id
email
role
department
team
supervisor
```

이번 프로젝트에서는 특히 다음 용도로 사용한다.

#### 사용자 연결

```text
email.user
    =
LDAP.user_id
```

#### 사용자 등록 이메일 확인

```text
email.from
    =
LDAP.email
```

#### 사내/외부 주소 판별

LDAP 등록 이메일은 `dtaa.com` 도메인으로 정리되어 있으므로,

```text
@dtaa.com
→ 사내 주소 후보

그 외 도메인
→ 외부 주소 후보
```

로 구분한다.

### 4) 가장 중요한 데이터 제약

현재 `email.csv`에는 발송/수신을 직접 구분하는 다음 컬럼이 없다.

```text
activity = Send / Receive
```

또한 `user`와 `from`이 항상 같은 것도 아니다.

공유 문서에서는 `from`과 해당 사용자의 LDAP 등록 이메일을 비교했을 때 다음과 같이 정리되어 있다.

- 일치: 819,522건
- 불일치: 494,878건
- 불일치 비율: 37.65%

따라서 이메일 행 전체를 곧바로 “해당 사용자가 보낸 메일”이라고 해석하면 안 된다.

### 5) 외부 발송 후보 이벤트 정의

1차 분석에서는 다음 조건을 만족하는 이벤트를 **외부 발송 후보**로 정의한다.

```text
조건 1
email.user = LDAP.user_id

조건 2
email.from = 해당 사용자의 LDAP 등록 email

조건 3
to / cc / bcc 중 하나 이상에
사내 도메인(dtaa.com)이 아닌 주소가 존재
```

정리하면:

```text
사용자 계정 확인
        +
LDAP 등록 발신주소 일치
        +
외부 수신자 존재
        ↓
외부 발송 후보 이벤트
```

이 조건은 실제 정보유출 확정 기준이 아니라 **분석 대상 후보를 보수적으로 선별하기 위한 기준**이다.

### 6) 분석 단위

1차 MVP의 기본 분석 단위는 다음과 같이 잡는다.

```text
사용자 × 일
```

이 단위에서 다음 지표를 집계한다.

- 외부 발송 후보 메일 수
- 외부 수신자 수
- 고유 외부 수신자 수
- 첨부 메일 수
- 첨부파일 개수 합계
- 외부 메일 `size` 합계·평균·최댓값
- 외부 BCC 사용
- 비정상 시간대 활동
- 신규 외부 수신자
- 신규 외부 도메인
- 동일 외부 수신자 반복 전송

정리하면 사용자×일 단위로 생성한 위 지표가 **X**이며, 현재 데이터에는 이를 악성/정상으로 판정해 주는 **Y가 없다**. 따라서 각 사용자의 개인 기준선 대비 이상도를 결합한 **Risk Score가 최종 Output**이 된다.

---

## d. 사용 도구 및 기술 스택 결정

공유 문서에서 제시된 도구와 현재 이메일 프로젝트 구조를 기준으로 다음과 같이 구성한다.

| 구분 | 사용 도구 / 방법 | 역할 |
|---|---|---|
| 로그 조회·입력 | `email.csv`, LDAP / 필요 시 OpenSearch | 원본 이메일 로그 및 사용자 정보 조회 |
| 데이터 처리 | Python | 전처리·집계·분석 로직 구현 |
| 데이터 분석 | Pandas | 이메일 파싱, 사용자×일 Feature 생성 |
| 수치 계산 | NumPy, SciPy | 통계량·Z-score 등 계산 |
| 이상탐지 | Z-score | 외부메일 수, 첨부 수, 메일 크기 등 수치형 지표의 개인 기준 이상도 |
| 신규성 탐지 | Novelty | 신규 외부 수신자, 신규 외부 도메인 등 |
| 규칙 기반 신호 | Rule | BCC, 시간대, 반복 전송 등 프로젝트 정의에 따른 보조 신호 |
| 위험점수 | Weighted Risk Score | 복수 이상신호 가중 결합 |
| 분석 문제 구조 | `X = 행동지표 / Y 없음 / Output = Risk Score` | 라벨 없는 행동 이상탐지 문제의 입력·정답·출력 관계 정의 |
| 비교 모델 | Isolation Forest **[선택 확장]** | 다변량 이상패턴 비교·검증 |
| 결과 화면 | Streamlit 또는 Jupyter | 위험 사용자 순위 및 점수 산정 근거 조회 |
| 협업 | GitHub | 코드 버전 관리 |
| 문서화 | Notion | 분석설계·회의·진행상황 공유 |

### 기술적 핵심 구조

```text
email + LDAP
    ↓
전처리 / 외부 발송 후보 추출
    ↓
사용자×일 Feature
    ↓
Baseline
    ↓
Z-score + Novelty
    ↓
점수 정규화
    ↓
가중 Risk Score
    ↓
Ranking
    ↓
산정 근거
```

이 구조에서 **X는 사용자×일 행동지표**, **Y는 현재 존재하지 않는 정상/악성 정답 라벨**, **최종 Output은 0~100 범위로 정규화할 Risk Score와 그 산정 근거**이다. Risk Score의 최종 범위와 등급 구간은 실제 분포를 확인한 뒤 팀 합의로 확정한다.

### [팀 작성 필요]

공유 문서만으로는 다음 내용이 확정되어 있지 않으므로 킥오프 전 팀에서 최종 결정한다.

- Streamlit과 Jupyter 중 최종 결과 화면 도구
- OpenSearch를 실제 분석 입력 단계에서 어느 수준까지 사용할지
- 각 위험지표의 최종 가중치
- Z-score 기준기간
- 근무시간 기준
- Isolation Forest를 2주 범위에 포함할지 여부

---

## e. 최종 산출 결과물

최종 결과물은 단순히 “위험 사용자”만 출력하는 것이 아니라, **위험순위와 산정 근거까지 확인할 수 있는 형태**로 구성한다.

> **X = 행동지표 / Y 없음 / 최종 Output = Risk Score**

따라서 결과 화면과 분석 문서에서는 특정 사용자를 악성으로 확정하지 않고, 어떤 행동지표가 평소와 달랐으며 그 신호가 Risk Score에 얼마나 기여했는지를 함께 제시한다.

### 1) 소스코드

- 이메일·LDAP 전처리 코드
- 외부 발송 후보 추출 코드
- 사용자×일 Feature 생성 코드
- Baseline 계산 코드
- Z-score / Novelty / Rule 이상도 계산 코드
- 위험점수 및 Ranking 코드
- 결과 화면 코드

### 2) 결과 화면

최소 다음 내용을 확인할 수 있도록 한다.

```text
사용자
위험점수
위험등급
위험순위
기준선 정보
주요 이상신호
현재값
평소값
점수 기여 근거
```

예시 형태:

```text
사용자: user_xxx
위험점수: 87 / 100
등급: 위험

[주요 근거]

1. 외부 발송 후보 이메일 증가
2. 신규 외부 수신자 발생
3. 첨부파일 증가
4. 메시지 크기 증가
5. 평소와 다른 시간대 활동
6. BCC 사용 증가
```

### 3) 분석설계서

다음 내용을 정리한다.

- 프로젝트 문제 정의
- 이메일 로그 구조
- 외부 발송 후보 정의
- Feature 정의
- X(행동지표) / Y 없음 / 최종 Output(Risk Score) 정의
- Baseline 정의
- 이상도 계산 방식
- 위험점수 산식
- 데이터 제약 및 해석 한계

### 4) 검증결과

공유 문서에 제시된 검증 구조를 이메일 프로젝트에 맞게 적용한다.

현재는 정답 라벨이 없으므로 검증의 목적은 “실제 악성 사용자를 맞혔는가”를 평가하는 것이 아니다. **X에 정상·단일 이상·복합 이상·반복 행동을 입력했을 때 최종 Output인 Risk Score가 상식적이고 일관된 방향으로 변하는지**를 확인한다.

> **검증 전제: X = 행동지표 / Y 없음 / 최종 Output = Risk Score**

#### 검증 시나리오 요약

| 구분 | 행동 예시(X) | 기대되는 Risk Score | 확인할 내용 |
|---|---|---|---|
| Case 1. 정상 행동 | 평소 수신자, 평소 메일 수·크기, 첨부 없음, 근무시간 발송 | 낮음 | 기준선과 비슷한 행동이 과도하게 경고되지 않는가 |
| Case 2. 단일 이상행동 | 신규 외부 수신자 1명만 추가되고 나머지는 평소와 동일 | 일부 증가 | 하나의 약한 신호가 점수에 반영되지만 곧바로 최고위험이 되지는 않는가 |
| Case 3. 복합 이상행동 | 외부메일·신규 수신자·첨부·크기·야간·BCC가 동시에 증가 | 높음 | 여러 신호가 결합될 때 단일 이상보다 점수가 충분히 높아지는가 |
| Case 4. 장기 반복 행동 | 같은 외부 수신자에게 첨부메일을 여러 날 반복 발송 | 누적 상승 | 일별 값이 크지 않아도 반복성과 지속성이 위험도에 반영되는가 |

#### 이해하기 쉬운 상세 예시

```text
Case 1. 정상 행동
예시)
- 사용자 A는 평소 하루 1~2건의 외부메일을 보냄
- 기존 거래처에 근무시간 중 발송
- 첨부파일 0개, 메일 크기도 평소 범위

예상 결과)
→ 대부분의 Z-score가 0에 가까움
→ 신규성·시간대·BCC 규칙 신호가 발생하지 않음
→ 낮은 Risk Score

Case 2. 단일 이상행동
예시)
- 평소와 같은 시간과 비슷한 크기의 메일
- 단, 처음 연락하는 외부 수신자 1명이 발생
- 첨부파일과 BCC는 없음

예상 결과)
→ 신규 수신자 신호만 Risk Score에 반영
→ Case 1보다는 점수가 높지만 즉시 고위험으로 판정하지 않음

Case 3. 복합 이상행동
예시)
- 평소 외부메일이 하루 1건인 사용자가 하루 8건 발송
- 신규 외부 도메인 3곳이 동시에 등장
- 첨부파일 5개, 메일 크기는 평소 평균의 약 3배
- 오후 11시 40분에 BCC를 사용해 발송

예상 결과)
→ 외부메일 수·첨부 수·size의 Z-score가 함께 상승
→ 신규성·비정상 시간·BCC 규칙 신호도 동시에 발생
→ Case 2보다 뚜렷하게 높은 Risk Score와 상위 위험순위
→ 화면에는 점수를 높인 각 신호와 기여도를 함께 표시

Case 4. 장기 반복 행동
예시)
- 특정 외부 수신자에게 10영업일 동안 매일 1건씩 전송
- 각 메일에는 1개의 첨부파일이 포함됨
- 하루 단위 메일 수는 아주 크지 않아 일별 Z-score만 보면 두드러지지 않을 수 있음

예상 결과)
→ 동일 수신자 반복 횟수와 지속일수 신호가 누적됨
→ 단발성 정상 메일과 구분하여 Risk Score가 점진적으로 상승
→ 사용자-외부 수신자 관계와 반복 전송 근거를 함께 제시
```

예를 들어 초기 가중치로 Case 1이 10점, Case 2가 30점, Case 3이 85점처럼 나타난다면 점수의 상대적 방향은 의도에 부합한다고 볼 수 있다. 이 숫자는 설명을 위한 예시이며, 실제 점수와 위험등급 기준은 전체 데이터 분포와 팀 합의를 통해 결정한다.

#### 검증 체크리스트

- **순서성:** 동일 사용자 기준으로 `정상 < 단일 이상 < 복합 이상` 순서로 Risk Score가 증가하는가
- **누적성:** 같은 행동이 장기간 반복되면 단발 행동보다 위험도가 높아지는가
- **설명 가능성:** 점수가 오른 이유를 X의 개별 지표와 기여도로 설명할 수 있는가
- **과잉 경보 방지:** 신규 수신자 1건 같은 단일 약신호만으로 최고위험 사용자가 되지 않는가
- **기준선 반영:** 동일 행동이라도 사용자의 평소 패턴에 따라 다른 점수가 계산되는가
- **재현성:** 같은 입력과 기준선에 대해 같은 Risk Score가 계산되는가

정답 라벨인 Y가 없으므로 이 단계에서는 정확도·정밀도·재현율·AUC를 핵심 성능지표로 사용하지 않는다. 향후 현재 데이터와 일치하는 answer key 또는 검증 라벨이 확보되면, 시나리오 검증에 더해 실제 탐지 성능 평가를 수행한다.

### 5) 발표자료

킥오프 이후 최종 발표에서는 다음 결과를 보여주는 것을 목표로 한다.

```text
보안 로그 입력
→ 사용자별 행동 기준선
→ 이상도 계산
→ 위험점수
→ 위험 사용자 순위
→ 주요 산정 근거
```

---

## f. 팀원 간의 역할 배분

현재 자료에는 **5명의 팀원 실명·역량·선호 역할 정보가 없으므로 실제 담당자는 확정할 수 없다.**

아래는 5명이 빠짐없이 핵심 영역을 맡을 수 있도록 나눈 **역할 구조 초안**이다. 실제 이름은 팀 회의에서 기입한다.

| 역할 | 주요 업무 | 핵심 산출물 | 담당자 |
|---|---|---|---|
| 역할 1. PM / 분석기획 | 프로젝트 범위 관리, NIA 사례와 문제정의 정리, 요구사항·발표 흐름 관리 | 기획안, 분석설계서, 일정 관리 | **[팀 작성 필요]** |
| 역할 2. 데이터 전처리 / LDAP 연계 | 이메일 주소 파싱, `user`-LDAP 연결, 발신 검증, 사내/외부 주소 판별 | 전처리 데이터, 외부 발송 후보 데이터 | **[팀 작성 필요]** |
| 역할 3. Feature / Baseline | 사용자×일 Feature 생성, 개인별 행동 기준선 구축 | Feature 테이블, Baseline 테이블 | **[팀 작성 필요]** |
| 역할 4. 이상도 / Risk Score / 검증 | Z-score·Novelty·Rule, 가중 위험점수, 정상·단일·복합·반복 시나리오 검증 | 위험점수 로직, Ranking, 검증결과 | **[팀 작성 필요]** |
| 역할 5. 결과 화면 / 통합 / 발표 | Streamlit 또는 Jupyter 결과 화면, 위험근거 시각화, 코드 통합, 발표자료 정리 | 결과 화면, 통합본, 발표자료 | **[팀 작성 필요]** |

### 역할 운영 원칙

- 역할은 완전히 분리하기보다 서로 코드와 산출물을 검토한다.
- Feature 정의와 위험점수 가중치는 한 명이 독단적으로 확정하지 않고 팀 합의로 결정한다.
- 데이터 해석에서 “외부 메일 = 유출”처럼 단정하지 않는 기준을 전원이 공유한다.

---

## g. 2주 간의 WBS

공유 자료에는 **2주의 실제 시작일·종료일과 팀원별 배정 일정이 없으므로**, 아래 WBS는 **10개 작업일 기준 초안**으로 작성한다.  
실제 날짜와 담당자 실명은 팀에서 기입한다.

### 1주차 — 데이터 이해 → Feature → Baseline

| 기간 | 주요 업무 | 세부 작업 | 산출물 | 담당 |
|---|---|---|---|---|
| 1일차 | 프로젝트 범위 확정 | 프로젝트명, 문제 정의, NIA 사례 연결, “유출 확정이 아닌 위험 사용자 우선순위화” 표현 합의 | 프로젝트 기획안 v1 | 역할 1 + 전원 |
| 2일차 | 이메일·LDAP 구조 확인 | 컬럼 확인, `to/cc/bcc` 파싱 규칙, `user`-LDAP 매핑, `from` 검증 조건 정의 | 데이터 처리 규칙서 | 역할 2 |
| 3일차 | 외부 발송 후보 추출 | `email.user = LDAP.user_id`, `email.from = LDAP.email`, 외부 수신자 조건 구현 | 외부 발송 후보 데이터 | 역할 2 |
| 4일차 | Feature 생성 | 외부메일 수, 외부 수신자, 첨부, `size`, BCC, 시간대, 신규 수신자·도메인, 반복 수신자 Feature 구현 | 사용자×일 Feature 테이블 | 역할 3 |
| 5일차 | Baseline·이상도 설계 | X(행동지표) 확정, Y 없음에 따른 검증 방식 정의, 사용자별 기준선 생성, Z-score 대상·Novelty 대상·Rule 대상 구분, 초기 임계값 검토 | Baseline 및 이상도 로직 v1 | 역할 3 + 역할 4 |

### 2주차 — 위험점수 → 검증 → 화면 → 발표

| 기간 | 주요 업무 | 세부 작업 | 산출물 | 담당 |
|---|---|---|---|---|
| 6일차 | 위험점수 구현 | X의 이상신호 정규화, 초기 가중치 설정, 최종 Output인 Weighted Risk Score 및 Ranking 구현 | Risk Score v1 | 역할 4 |
| 7일차 | 시나리오 검증 | Y가 없는 조건에서 정상 / 단일 이상 / 복합 이상 / 장기 반복 행동에 대한 Risk Score의 순서성·누적성·설명 가능성 확인 | 검증결과 v1 | 역할 4 + 전원 |
| 8일차 | 결과 화면 구현 | 위험 사용자 순위, 점수, 기준선, 주요 기여 신호를 확인할 수 있는 화면 구현 | Streamlit 또는 Jupyter 결과 화면 | 역할 5 |
| 9일차 | 통합·수정 | 전처리→Feature→Baseline→Risk Score→화면 전체 연결, 오류 수정, 설명 문구 정리 | 통합 분석 시스템 v1 | 역할 2~5 + 전원 검토 |
| 10일차 | 최종 정리·발표 준비 | 소스코드 정리, 분석설계서·검증결과 정리, 발표자료 및 시연 흐름 완성 | 최종 산출물 패키지 | 역할 1 + 역할 5 + 전원 |

### WBS에서 팀이 추가로 확정해야 할 사항

- **[팀 작성 필요]** 실제 2주 시작일 / 종료일
- **[팀 작성 필요]** 역할 1~5의 실제 담당자 이름
- **[팀 작성 필요]** 각 작업의 완료 기준
- **[팀 작성 필요]** Baseline 기간
- **[팀 작성 필요]** 근무시간 기준
